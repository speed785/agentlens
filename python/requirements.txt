# Core — AgentLens has zero required dependencies
# Optional integrations:
# openai>=1.0.0
# anthropic>=0.20.0

# Development / testing
pytest>=7.0
pytest-asyncio>=0.21
black>=23.0
mypy>=1.0
ruff>=0.1
